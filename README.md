# Perform 2026: Intro to DQL, Part 2

## Prior to the lab:

The user will have to execute initialize.sh

``` bash
chmod +x initialize.sh
./initialize.sh
```
And also download and import the notebooks and launchpad in the git (dynatraceImports.zip): https://github.com/Dynatrace-PDP/dtu_sandbox_dql

They will also have to import the lookup tables in the dynatraceImports.zip where the notebooks and launchpad are also located.